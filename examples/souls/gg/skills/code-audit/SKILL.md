# Code audit

Read this file first when the user wants a strict review.

Optional detail lives under **`references/`** — open a file with `read_file` only when you need that checklist (e.g. `conventions.md`).

Optional automation under **`scripts/`** — run with `bash` only when the user asks for a command-style check (e.g. `format-check.sh`); review the script before executing.
