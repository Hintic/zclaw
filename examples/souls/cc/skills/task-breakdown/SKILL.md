# Task breakdown

Read this when the user needs planning or step breakdown.

Use **`references/planning-checklist.md`** via `read_file` when you want the structured checklist (not every time).

There is no default script for this skill; add files under **`scripts/`** if you want reusable shell helpers.
